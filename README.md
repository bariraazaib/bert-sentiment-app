# bert-sentiment-app
%%writefile README.md
# Customer Feedback Sentiment Classifier (BERT)

This Streamlit app uses a fine-tuned BERT model to classify customer feedback into Positive, Neutral, or Negative.

## Run Locally
```bash
pip install -r requirements.txt
streamlit run app.py
